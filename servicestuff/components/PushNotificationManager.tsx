import React from 'react';

// Push Notifications disabled - Firebase/FCM is not used in this project.
// Real-time updates are handled via Socket.io instead.
export const PushNotificationManager: React.FC = () => {
    return null;
};
