generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema"]
  engineType      = "library"
}

datasource db {
  provider = "postgresql"
  schemas  = ["auth", "public"]
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model audit_log_entries {
  instance_id String?   @db.Uuid
  id          String    @id @db.Uuid
  payload     Json?     @db.Json
  created_at  DateTime? @db.Timestamptz(6)
  ip_address  String    @default("") @db.VarChar(64)

  @@index([instance_id], map: "audit_logs_instance_id_idx")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model flow_state {
  id                     String                @id @db.Uuid
  user_id                String?               @db.Uuid
  auth_code              String
  code_challenge_method  code_challenge_method
  code_challenge         String
  provider_type          String
  provider_access_token  String?
  provider_refresh_token String?
  created_at             DateTime?             @db.Timestamptz(6)
  updated_at             DateTime?             @db.Timestamptz(6)
  authentication_method  String
  auth_code_issued_at    DateTime?             @db.Timestamptz(6)
  saml_relay_states      saml_relay_states[]

  @@index([created_at(sort: Desc)])
  @@index([auth_code], map: "idx_auth_code")
  @@index([user_id, authentication_method], map: "idx_user_id_auth_method")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model identities {
  provider_id     String
  user_id         String    @db.Uuid
  identity_data   Json
  provider        String
  last_sign_in_at DateTime? @db.Timestamptz(6)
  created_at      DateTime? @db.Timestamptz(6)
  updated_at      DateTime? @db.Timestamptz(6)
  email           String?   @default(dbgenerated("lower((identity_data ->> 'email'::text))"))
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  users           users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([provider_id, provider], map: "identities_provider_id_provider_unique")
  @@index([email])
  @@index([user_id])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model instances {
  id              String    @id @db.Uuid
  uuid            String?   @db.Uuid
  raw_base_config String?
  created_at      DateTime? @db.Timestamptz(6)
  updated_at      DateTime? @db.Timestamptz(6)

  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_amr_claims {
  session_id            String   @db.Uuid
  created_at            DateTime @db.Timestamptz(6)
  updated_at            DateTime @db.Timestamptz(6)
  authentication_method String
  id                    String   @id(map: "amr_id_pk") @db.Uuid
  sessions              sessions @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([session_id, authentication_method], map: "mfa_amr_claims_session_id_authentication_method_pkey")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_challenges {
  id                     String      @id @db.Uuid
  factor_id              String      @db.Uuid
  created_at             DateTime    @db.Timestamptz(6)
  verified_at            DateTime?   @db.Timestamptz(6)
  ip_address             String      @db.Inet
  otp_code               String?
  web_authn_session_data Json?
  mfa_factors            mfa_factors @relation(fields: [factor_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "mfa_challenges_auth_factor_id_fkey")

  @@index([created_at(sort: Desc)], map: "mfa_challenge_created_at_idx")
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model mfa_factors {
  id                           String           @id @db.Uuid
  user_id                      String           @db.Uuid
  friendly_name                String?
  factor_type                  factor_type
  status                       factor_status
  created_at                   DateTime         @db.Timestamptz(6)
  updated_at                   DateTime         @db.Timestamptz(6)
  secret                       String?
  phone                        String?
  last_challenged_at           DateTime?        @unique @db.Timestamptz(6)
  web_authn_credential         Json?
  web_authn_aaguid             String?          @db.Uuid
  last_webauthn_challenge_data Json?
  mfa_challenges               mfa_challenges[]
  users                        users            @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, phone], map: "unique_phone_factor_per_user")
  @@index([user_id, created_at], map: "factor_id_created_at_idx")
  @@index([user_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_authorizations {
  id                    String                     @id @db.Uuid
  authorization_id      String                     @unique
  client_id             String                     @db.Uuid
  user_id               String?                    @db.Uuid
  redirect_uri          String
  scope                 String
  state                 String?
  resource              String?
  code_challenge        String?
  code_challenge_method code_challenge_method?
  response_type         oauth_response_type        @default(code)
  status                oauth_authorization_status @default(pending)
  authorization_code    String?                    @unique
  created_at            DateTime                   @default(now()) @db.Timestamptz(6)
  expires_at            DateTime                   @default(dbgenerated("(now() + '00:03:00'::interval)")) @db.Timestamptz(6)
  approved_at           DateTime?                  @db.Timestamptz(6)
  nonce                 String?
  oauth_clients         oauth_clients              @relation(fields: [client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                 users?                     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model oauth_client_states {
  id            String   @id @db.Uuid
  provider_type String
  code_verifier String?
  created_at    DateTime @db.Timestamptz(6)

  @@index([created_at], map: "idx_oauth_client_states_created_at")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_clients {
  id                   String                  @id @db.Uuid
  client_secret_hash   String?
  registration_type    oauth_registration_type
  redirect_uris        String
  grant_types          String
  client_name          String?
  client_uri           String?
  logo_uri             String?
  created_at           DateTime                @default(now()) @db.Timestamptz(6)
  updated_at           DateTime                @default(now()) @db.Timestamptz(6)
  deleted_at           DateTime?               @db.Timestamptz(6)
  client_type          oauth_client_type       @default(confidential)
  oauth_authorizations oauth_authorizations[]
  oauth_consents       oauth_consents[]
  sessions             sessions[]

  @@index([deleted_at])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model oauth_consents {
  id            String        @id @db.Uuid
  user_id       String        @db.Uuid
  client_id     String        @db.Uuid
  scopes        String
  granted_at    DateTime      @default(now()) @db.Timestamptz(6)
  revoked_at    DateTime?     @db.Timestamptz(6)
  oauth_clients oauth_clients @relation(fields: [client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users         users         @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, client_id], map: "oauth_consents_user_client_unique")
  @@index([user_id, granted_at(sort: Desc)], map: "oauth_consents_user_order_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model one_time_tokens {
  id         String              @id @db.Uuid
  user_id    String              @db.Uuid
  token_type one_time_token_type
  token_hash String
  relates_to String
  created_at DateTime            @default(now()) @db.Timestamp(6)
  updated_at DateTime            @default(now()) @db.Timestamp(6)
  users      users               @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([user_id, token_type])
  @@index([relates_to], map: "one_time_tokens_relates_to_hash_idx", type: Hash)
  @@index([token_hash], map: "one_time_tokens_token_hash_hash_idx", type: Hash)
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model refresh_tokens {
  instance_id String?   @db.Uuid
  id          BigInt    @id @default(autoincrement())
  token       String?   @unique(map: "refresh_tokens_token_unique") @db.VarChar(255)
  user_id     String?   @db.VarChar(255)
  revoked     Boolean?
  created_at  DateTime? @db.Timestamptz(6)
  updated_at  DateTime? @db.Timestamptz(6)
  parent      String?   @db.VarChar(255)
  session_id  String?   @db.Uuid
  sessions    sessions? @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([instance_id])
  @@index([instance_id, user_id])
  @@index([parent])
  @@index([session_id, revoked])
  @@index([updated_at(sort: Desc)])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model saml_providers {
  id                String        @id @db.Uuid
  sso_provider_id   String        @db.Uuid
  entity_id         String        @unique
  metadata_xml      String
  metadata_url      String?
  attribute_mapping Json?
  created_at        DateTime?     @db.Timestamptz(6)
  updated_at        DateTime?     @db.Timestamptz(6)
  name_id_format    String?
  sso_providers     sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([sso_provider_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model saml_relay_states {
  id              String        @id @db.Uuid
  sso_provider_id String        @db.Uuid
  request_id      String
  for_email       String?
  redirect_to     String?
  created_at      DateTime?     @db.Timestamptz(6)
  updated_at      DateTime?     @db.Timestamptz(6)
  flow_state_id   String?       @db.Uuid
  flow_state      flow_state?   @relation(fields: [flow_state_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sso_providers   sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([created_at(sort: Desc)])
  @@index([for_email])
  @@index([sso_provider_id])
  @@schema("auth")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model schema_migrations {
  version String @id @db.VarChar(255)

  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sessions {
  id                     String           @id @db.Uuid
  user_id                String           @db.Uuid
  created_at             DateTime?        @db.Timestamptz(6)
  updated_at             DateTime?        @db.Timestamptz(6)
  factor_id              String?          @db.Uuid
  aal                    aal_level?
  not_after              DateTime?        @db.Timestamptz(6)
  refreshed_at           DateTime?        @db.Timestamp(6)
  user_agent             String?
  ip                     String?          @db.Inet
  tag                    String?
  oauth_client_id        String?          @db.Uuid
  refresh_token_hmac_key String?
  refresh_token_counter  BigInt?
  scopes                 String?
  mfa_amr_claims         mfa_amr_claims[]
  refresh_tokens         refresh_tokens[]
  oauth_clients          oauth_clients?   @relation(fields: [oauth_client_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users                  users            @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([not_after(sort: Desc)])
  @@index([oauth_client_id])
  @@index([user_id])
  @@index([user_id, created_at], map: "user_id_created_at_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model sso_domains {
  id              String        @id @db.Uuid
  sso_provider_id String        @db.Uuid
  domain          String
  created_at      DateTime?     @db.Timestamptz(6)
  updated_at      DateTime?     @db.Timestamptz(6)
  sso_providers   sso_providers @relation(fields: [sso_provider_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([sso_provider_id])
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model sso_providers {
  id                String              @id @db.Uuid
  resource_id       String?
  created_at        DateTime?           @db.Timestamptz(6)
  updated_at        DateTime?           @db.Timestamptz(6)
  disabled          Boolean?
  saml_providers    saml_providers[]
  saml_relay_states saml_relay_states[]
  sso_domains       sso_domains[]

  @@index([resource_id], map: "sso_providers_resource_id_pattern_idx")
  @@schema("auth")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model users {
  instance_id                 String?                @db.Uuid
  id                          String                 @id @db.Uuid
  aud                         String?                @db.VarChar(255)
  role                        String?                @db.VarChar(255)
  email                       String?                @db.VarChar(255)
  encrypted_password          String?                @db.VarChar(255)
  email_confirmed_at          DateTime?              @db.Timestamptz(6)
  invited_at                  DateTime?              @db.Timestamptz(6)
  confirmation_token          String?                @db.VarChar(255)
  confirmation_sent_at        DateTime?              @db.Timestamptz(6)
  recovery_token              String?                @db.VarChar(255)
  recovery_sent_at            DateTime?              @db.Timestamptz(6)
  email_change_token_new      String?                @db.VarChar(255)
  email_change                String?                @db.VarChar(255)
  email_change_sent_at        DateTime?              @db.Timestamptz(6)
  last_sign_in_at             DateTime?              @db.Timestamptz(6)
  raw_app_meta_data           Json?
  raw_user_meta_data          Json?
  is_super_admin              Boolean?
  created_at                  DateTime?              @db.Timestamptz(6)
  updated_at                  DateTime?              @db.Timestamptz(6)
  phone                       String?                @unique
  phone_confirmed_at          DateTime?              @db.Timestamptz(6)
  phone_change                String?                @default("")
  phone_change_token          String?                @default("") @db.VarChar(255)
  phone_change_sent_at        DateTime?              @db.Timestamptz(6)
  confirmed_at                DateTime?              @default(dbgenerated("LEAST(email_confirmed_at, phone_confirmed_at)")) @db.Timestamptz(6)
  email_change_token_current  String?                @default("") @db.VarChar(255)
  email_change_confirm_status Int?                   @default(0) @db.SmallInt
  banned_until                DateTime?              @db.Timestamptz(6)
  reauthentication_token      String?                @default("") @db.VarChar(255)
  reauthentication_sent_at    DateTime?              @db.Timestamptz(6)
  is_sso_user                 Boolean                @default(false)
  deleted_at                  DateTime?              @db.Timestamptz(6)
  is_anonymous                Boolean                @default(false)
  identities                  identities[]
  mfa_factors                 mfa_factors[]
  oauth_authorizations        oauth_authorizations[]
  oauth_consents              oauth_consents[]
  one_time_tokens             one_time_tokens[]
  sessions                    sessions[]

  @@index([instance_id])
  @@index([is_anonymous])
  @@schema("auth")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model audit_logs {
  id         String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id    String?   @db.Uuid
  action     String    @db.VarChar(100)
  module     String    @db.VarChar(50)
  entity_id  String?   @db.Uuid
  old_data   Json?
  new_data   Json?
  ip_address String?   @db.VarChar(45)
  created_at DateTime? @default(now()) @db.Timestamptz(6)
  profiles   profiles? @relation(fields: [user_id], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model banners {
  id         String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  title      String?   @db.VarChar(255)
  image_url  String
  link_url   String?
  position   String?   @db.VarChar(50)
  is_active  Boolean?  @default(true)
  starts_at  DateTime? @db.Timestamptz(6)
  ends_at    DateTime? @db.Timestamptz(6)
  created_at DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model bike_models {
  id                  String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                String                @unique @db.VarChar(100)
  cc                  String?               @db.VarChar(10)
  engine_type         String?               @db.VarChar(100)
  brand               String?               @default("Suzuki") @db.VarChar(50)
  is_active           Boolean?              @default(true)
  created_at          DateTime?             @default(now()) @db.Timestamptz(6)
  updated_at          DateTime?             @default(now()) @db.Timestamptz(6)
  code                String?               @unique @db.VarChar(50)
  image_url           String?
  product_bike_models product_bike_models[]
  service_vehicles    service_vehicles[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model brands {
  id            String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name          String     @unique @db.VarChar
  slug          String?    @unique @db.VarChar
  logo_url      String?
  country       String?    @db.VarChar
  description   String?
  website       String?    @db.VarChar
  is_active     Boolean?   @default(true)
  display_order Int?       @default(0)
  created_at    DateTime?  @default(now()) @db.Timestamptz(6)
  updated_at    DateTime?  @default(now()) @db.Timestamptz(6)
  dealer_id     String?    @db.Uuid
  dealers       dealers?   @relation(fields: [dealer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  products      products[]

  @@index([slug], map: "idx_brands_slug")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model campaign_analytics {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  campaign_id   String?   @db.Uuid
  campaign_type String?   @db.VarChar(50)
  views         Int?      @default(0)
  clicks        Int?      @default(0)
  conversions   Int?      @default(0)
  created_at    DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model cart_items {
  id               String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  cart_id          String?           @db.Uuid
  product_id       String?           @db.Uuid
  variant_id       String?           @db.Uuid
  quantity         Int               @default(1)
  created_at       DateTime?         @default(now()) @db.Timestamptz(6)
  updated_at       DateTime?         @default(now()) @db.Timestamptz(6)
  carts            carts?            @relation(fields: [cart_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  products         products?         @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  product_variants product_variants? @relation(fields: [variant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model carts {
  id         String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id    String?      @db.Uuid
  guest_id   String?      @db.VarChar(255)
  created_at DateTime?    @default(now()) @db.Timestamptz(6)
  updated_at DateTime?    @default(now()) @db.Timestamptz(6)
  cart_items cart_items[]
  profiles   profiles?    @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model categories {
  id                                            String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                                          String                   @db.VarChar(100)
  slug                                          String                   @unique @db.VarChar(100)
  parent_id                                     String?                  @db.Uuid
  description                                   String?
  icon_url                                      String?
  image_url                                     String?
  level                                         Int?                     @default(0)
  is_active                                     Boolean?                 @default(true)
  created_at                                    DateTime?                @default(now()) @db.Timestamptz(6)
  updated_at                                    DateTime?                @default(now()) @db.Timestamptz(6)
  categories                                    categories?              @relation("categoriesTocategories", fields: [parent_id], references: [id], onUpdate: NoAction)
  other_categories                              categories[]             @relation("categoriesTocategories")
  product_attribute_sets                        product_attribute_sets[]
  products_products_category_idTocategories     products[]               @relation("products_category_idTocategories")
  products_products_sub_category_idTocategories products[]               @relation("products_sub_category_idTocategories")

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model commissions {
  id           String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  order_id     String?     @db.Uuid
  sub_order_id String?     @db.Uuid
  dealer_id    String?     @db.Uuid
  amount       Decimal     @db.Decimal(12, 2)
  rate         Decimal     @db.Decimal(5, 2)
  status       String?     @default("pending") @db.VarChar(20)
  created_at   DateTime?   @default(now()) @db.Timestamptz(6)
  dealers      dealers?    @relation(fields: [dealer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  orders       orders?     @relation(fields: [order_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sub_orders   sub_orders? @relation(fields: [sub_order_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model coupons {
  id           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  code         String    @unique @db.VarChar(50)
  type         String?   @db.VarChar(20)
  value        Decimal   @db.Decimal(12, 2)
  min_spend    Decimal?  @default(0) @db.Decimal(12, 2)
  max_discount Decimal?  @db.Decimal(12, 2)
  starts_at    DateTime  @db.Timestamptz(6)
  ends_at      DateTime  @db.Timestamptz(6)
  usage_limit  Int?
  used_count   Int?      @default(0)
  dealer_id    String?   @db.Uuid
  is_active    Boolean?  @default(true)
  created_at   DateTime? @default(now()) @db.Timestamptz(6)
  dealers      dealers?  @relation(fields: [dealer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model customer_addresses {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id       String?   @db.Uuid
  name          String    @db.VarChar(255)
  phone         String    @db.VarChar(20)
  address_line1 String
  address_line2 String?
  city          String?   @db.VarChar(100)
  district      String?   @db.VarChar(100)
  division      String?   @db.VarChar(100)
  postal_code   String?   @db.VarChar(20)
  is_default    Boolean?  @default(false)
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  updated_at    DateTime? @default(now()) @db.Timestamptz(6)
  profiles      profiles? @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  orders        orders[]

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model customer_ledger {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  dealer_id        String    @db.Uuid
  customer_id      String    @db.Uuid
  transaction_type String?   @db.VarChar
  reference_type   String?   @db.VarChar
  reference_id     String?   @db.Uuid
  reference_number String?   @db.VarChar
  debit            Decimal?  @default(0) @db.Decimal
  credit           Decimal?  @default(0) @db.Decimal
  balance          Decimal?  @db.Decimal
  description      String?
  transaction_date DateTime  @default(dbgenerated("CURRENT_DATE")) @db.Date
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  profiles         profiles  @relation(fields: [customer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  dealers          dealers   @relation(fields: [dealer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([customer_id], map: "idx_cl_customer")
  @@index([transaction_date(sort: Desc)], map: "idx_cl_date")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model dealer_ads {
  id         String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  dealer_id  String    @db.Uuid
  title      String
  image_url  String
  link       String?
  status     String?   @default("active")
  created_at DateTime? @default(now()) @db.Timestamptz(6)
  updated_at DateTime? @default(now()) @db.Timestamptz(6)
  dealers    dealers   @relation(fields: [dealer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model dealer_bank_accounts {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  dealer_id      String?   @db.Uuid
  account_type   String?   @db.VarChar(20)
  provider_name  String?   @db.VarChar(100)
  account_name   String?   @db.VarChar(255)
  account_number String?   @db.VarChar(100)
  branch_name    String?   @db.VarChar(100)
  routing_number String?   @db.VarChar(50)
  is_primary     Boolean?  @default(false)
  created_at     DateTime? @default(now()) @db.Timestamptz(6)
  updated_at     DateTime? @default(now()) @db.Timestamptz(6)
  dealers        dealers?  @relation(fields: [dealer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model dealer_documents {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  dealer_id        String?   @db.Uuid
  document_type    String    @db.VarChar(50)
  document_url     String
  status           String?   @default("pending") @db.VarChar(20)
  rejection_reason String?
  verified_at      DateTime? @db.Timestamptz(6)
  verified_by      String?   @db.Uuid
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  dealers          dealers?  @relation(fields: [dealer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles         profiles? @relation(fields: [verified_by], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model dealer_notifications {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  dealer_id         String?   @db.Uuid
  notification_type String    @db.VarChar(50)
  is_enabled        Boolean?  @default(true)
  created_at        DateTime? @default(now()) @db.Timestamptz(6)
  updated_at        DateTime? @default(now()) @db.Timestamptz(6)
  dealers           dealers?  @relation(fields: [dealer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([dealer_id, notification_type])
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model dealer_payouts {
  id            String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  dealer_id     String?        @db.Uuid
  amount        Decimal        @db.Decimal(15, 2)
  status        String?        @default("pending") @db.VarChar(20)
  payout_method String?        @db.VarChar(50)
  reference_id  String?        @db.VarChar(100)
  processed_at  DateTime?      @db.Timestamptz(6)
  processed_by  String?        @db.Uuid
  created_at    DateTime?      @default(now()) @db.Timestamptz(6)
  dealers       dealers?       @relation(fields: [dealer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles      profiles?      @relation(fields: [processed_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  payout_items  payout_items[]

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model dealer_reviews {
  id                   String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  dealer_id            String    @db.Uuid
  user_id              String?   @db.Uuid
  rating               Int
  comment              String?
  is_verified_purchase Boolean?  @default(false)
  created_at           DateTime? @default(now()) @db.Timestamptz(6)
  updated_at           DateTime? @default(now()) @db.Timestamptz(6)
  dealers              dealers   @relation(fields: [dealer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles             profiles? @relation(fields: [user_id], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model dealer_settings {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  dealer_id     String?   @db.Uuid
  setting_key   String    @db.VarChar(100)
  setting_value Json?
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  updated_at    DateTime? @default(now()) @db.Timestamptz(6)
  dealers       dealers?  @relation(fields: [dealer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([dealer_id, setting_key])
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model dealer_users {
  id                   String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  dealer_id            String?   @db.Uuid
  user_id              String?   @db.Uuid
  dealer_role          String?   @db.VarChar(20)
  can_manage_products  Boolean?  @default(false)
  can_manage_orders    Boolean?  @default(false)
  can_view_reports     Boolean?  @default(false)
  can_manage_inventory Boolean?  @default(false)
  can_manage_coupons   Boolean?  @default(false)
  can_manage_staff     Boolean?  @default(false)
  can_view_finance     Boolean?  @default(false)
  can_reply_reviews    Boolean?  @default(false)
  created_at           DateTime? @default(now()) @db.Timestamptz(6)
  updated_at           DateTime? @default(now()) @db.Timestamptz(6)
  dealers              dealers?  @relation(fields: [dealer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles             profiles? @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([dealer_id, user_id])
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model dealers {
  id                                       String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  business_name                            String                  @db.VarChar(255)
  slug                                     String                  @unique @db.VarChar(255)
  email                                    String                  @unique @db.VarChar(255)
  phone                                    String                  @unique @db.VarChar(20)
  trade_license_no                         String?                 @db.VarChar(100)
  tin_number                               String?                 @db.VarChar(100)
  bin_number                               String?                 @db.VarChar(100)
  logo_url                                 String?
  banner_url                               String?
  description                              String?
  address_line1                            String?
  city                                     String?                 @db.VarChar(100)
  district                                 String?                 @db.VarChar(100)
  division                                 String?                 @db.VarChar(100)
  owner_user_id                            String?                 @db.Uuid
  status                                   String?                 @default("pending") @db.VarChar(20)
  current_plan_id                          String?                 @db.Uuid
  subscription_status                      String?                 @default("active") @db.VarChar(20)
  trial_ends_at                            DateTime?               @db.Timestamptz(6)
  commission_rate                          Decimal?                @db.Decimal(5, 2)
  commission_type                          String?                 @default("percentage") @db.VarChar(20)
  max_products                             Int?
  max_users                                Int?
  max_images_per_product                   Int?                    @default(5)
  total_products                           Int?                    @default(0)
  total_orders                             Int?                    @default(0)
  total_revenue                            Decimal?                @default(0) @db.Decimal(15, 2)
  average_rating                           Decimal?                @default(0) @db.Decimal(3, 2)
  created_at                               DateTime?               @default(now()) @db.Timestamptz(6)
  updated_at                               DateTime?               @default(now()) @db.Timestamptz(6)
  social_links                             Json?                   @default("{}")
  pinned_product_ids                       String[]                @default([]) @db.Uuid
  cover_photo_url                          String?
  brands                                   brands[]
  commissions                              commissions[]
  coupons                                  coupons[]
  customer_ledger                          customer_ledger[]
  dealer_ads                               dealer_ads[]
  dealer_bank_accounts                     dealer_bank_accounts[]
  dealer_documents                         dealer_documents[]
  dealer_notifications                     dealer_notifications[]
  dealer_payouts                           dealer_payouts[]
  dealer_reviews                           dealer_reviews[]
  dealer_settings                          dealer_settings[]
  dealer_users                             dealer_users[]
  plans                                    plans?                  @relation(fields: [current_plan_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  profiles_dealers_owner_user_idToprofiles profiles?               @relation("dealers_owner_user_idToprofiles", fields: [owner_user_id], references: [id], onUpdate: NoAction)
  expenses                                 expenses[]
  import_logs                              import_logs[]
  inventory_batches                        inventory_batches[]
  inventory_movements                      inventory_movements[]
  order_items                              order_items[]
  payment_transactions                     payment_transactions[]
  products                                 products[]
  profiles_profiles_dealer_idTodealers     profiles[]              @relation("profiles_dealer_idTodealers")
  purchase_orders                          purchase_orders[]
  sales                                    sales[]
  stock_adjustments                        stock_adjustments[]
  stock_locations                          stock_locations[]
  sub_orders                               sub_orders[]
  subscription_invoices                    subscription_invoices[]
  subscriptions                            subscriptions[]
  support_tickets                          support_tickets[]
  transactions                             transactions[]
  vendor_ledger                            vendor_ledger[]
  vendors                                  vendors[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model email_templates {
  id         String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name       String    @unique @db.VarChar(100)
  subject    String?   @db.VarChar(255)
  body_html  String?
  created_at DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model expense_categories {
  id                       String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                     String               @db.VarChar
  description              String?
  parent_id                String?              @db.Uuid
  is_active                Boolean?             @default(true)
  created_at               DateTime?            @default(now()) @db.Timestamptz(6)
  expense_categories       expense_categories?  @relation("expense_categoriesToexpense_categories", fields: [parent_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  other_expense_categories expense_categories[] @relation("expense_categoriesToexpense_categories")
  expenses                 expenses[]

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model expenses {
  id                                      String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  expense_number                          String                @db.VarChar
  dealer_id                               String                @db.Uuid
  category_id                             String?               @db.Uuid
  title                                   String                @db.VarChar
  description                             String?
  amount                                  Decimal               @db.Decimal
  payment_method                          String?               @db.VarChar
  payment_transaction_id                  String?               @db.Uuid
  vendor_id                               String?               @db.Uuid
  receipt_url                             String?
  expense_date                            DateTime              @default(dbgenerated("CURRENT_DATE")) @db.Date
  status                                  String?               @default("paid") @db.VarChar
  approved_by                             String?               @db.Uuid
  created_by                              String?               @db.Uuid
  created_at                              DateTime?             @default(now()) @db.Timestamptz(6)
  profiles_expenses_approved_byToprofiles profiles?             @relation("expenses_approved_byToprofiles", fields: [approved_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  expense_categories                      expense_categories?   @relation(fields: [category_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  profiles_expenses_created_byToprofiles  profiles?             @relation("expenses_created_byToprofiles", fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  dealers                                 dealers               @relation(fields: [dealer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  payment_transactions                    payment_transactions? @relation(fields: [payment_transaction_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  vendors                                 vendors?              @relation(fields: [vendor_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([dealer_id, expense_number])
  @@index([category_id], map: "idx_expenses_category")
  @@index([expense_date(sort: Desc)], map: "idx_expenses_date")
  @@index([dealer_id], map: "idx_expenses_dealer")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model faqs {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  question      String
  answer        String
  category      String?   @db.VarChar(100)
  is_active     Boolean?  @default(true)
  display_order Int?      @default(0)
  created_at    DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model feature_flags {
  id          String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name        String    @unique @db.VarChar(100)
  is_enabled  Boolean?  @default(false)
  description String?
  created_at  DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model import_logs {
  id              String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  dealer_id       String    @db.Uuid
  filename        String    @db.VarChar
  total_rows      Int
  successful_rows Int?      @default(0)
  failed_rows     Int?      @default(0)
  error_details   Json?
  status          String?   @default("processing") @db.VarChar
  created_at      DateTime? @default(now()) @db.Timestamptz(6)
  completed_at    DateTime? @db.Timestamptz(6)
  dealers         dealers   @relation(fields: [dealer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model inventory {
  id               String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  product_id       String?           @db.Uuid
  variant_id       String?           @db.Uuid
  change_quantity  Int
  type             String            @db.VarChar(50)
  description      String?
  reference_id     String?           @db.Uuid
  created_at       DateTime?         @default(now()) @db.Timestamptz(6)
  products         products?         @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  product_variants product_variants? @relation(fields: [variant_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model inventory_batches {
  id                     String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  dealer_id              String                   @db.Uuid
  product_id             String                   @db.Uuid
  variant_id             String?                  @db.Uuid
  batch_number           String                   @db.VarChar
  barcode                String?                  @unique @db.VarChar
  purchase_order_id      String?                  @db.Uuid
  purchase_order_item_id String?                  @db.Uuid
  vendor_id              String?                  @db.Uuid
  initial_quantity       Int
  current_quantity       Int                      @default(0)
  sold_quantity          Int?                     @default(0)
  damaged_quantity       Int?                     @default(0)
  returned_quantity      Int?                     @default(0)
  unit_cost_price        Decimal                  @db.Decimal
  manufacturing_date     DateTime?                @db.Date
  expiry_date            DateTime?                @db.Date
  received_date          DateTime                 @default(dbgenerated("CURRENT_DATE")) @db.Date
  warehouse_location     String?                  @db.VarChar
  rack_number            String?                  @db.VarChar
  bin_number             String?                  @db.VarChar
  status                 String?                  @default("active") @db.VarChar
  low_stock_threshold    Int?                     @default(5)
  created_at             DateTime?                @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?                @default(now()) @db.Timestamptz(6)
  dealers                dealers                  @relation(fields: [dealer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  products               products                 @relation(fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  purchase_orders        purchase_orders?         @relation(fields: [purchase_order_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  purchase_order_items   purchase_order_items?    @relation(fields: [purchase_order_item_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  product_variants       product_variants?        @relation(fields: [variant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  vendors                vendors?                 @relation(fields: [vendor_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  inventory_movements    inventory_movements[]
  sale_items             sale_items[]
  stock_adjustment_items stock_adjustment_items[]

  @@unique([dealer_id, batch_number])
  @@index([dealer_id], map: "idx_batches_dealer")
  @@index([variant_id], map: "idx_batches_variant")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model inventory_movements {
  id                String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  dealer_id         String             @db.Uuid
  product_id        String             @db.Uuid
  variant_id        String?            @db.Uuid
  batch_id          String?            @db.Uuid
  movement_type     String             @db.VarChar
  quantity_before   Int
  quantity_change   Int
  quantity_after    Int
  reference_type    String?            @db.VarChar
  reference_id      String?            @db.Uuid
  reference_number  String?            @db.VarChar
  unit_cost         Decimal?           @db.Decimal
  total_value       Decimal?           @db.Decimal
  performed_by      String?            @db.Uuid
  movement_date     DateTime           @default(dbgenerated("CURRENT_DATE")) @db.Date
  movement_time     DateTime           @default(dbgenerated("CURRENT_TIME")) @db.Time(6)
  reason            String?
  notes             String?
  created_at        DateTime?          @default(now()) @db.Timestamptz(6)
  inventory_batches inventory_batches? @relation(fields: [batch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  dealers           dealers            @relation(fields: [dealer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  profiles          profiles?          @relation(fields: [performed_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  products          products           @relation(fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  product_variants  product_variants?  @relation(fields: [variant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([batch_id], map: "idx_movements_batch")
  @@index([movement_date(sort: Desc)], map: "idx_movements_date")
  @@index([dealer_id], map: "idx_movements_dealer")
  @@index([movement_type], map: "idx_movements_type")
  @@index([variant_id], map: "idx_movements_variant")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model notification_templates {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name           String    @unique @db.VarChar(100)
  title_template String?
  body_template  String?
  created_at     DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model notifications {
  id         String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id    String?   @db.Uuid
  title      String    @db.VarChar(255)
  message    String
  type       String?   @db.VarChar(50)
  is_read    Boolean?  @default(false)
  link_url   String?
  created_at DateTime? @default(now()) @db.Timestamptz(6)
  profiles   profiles? @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model order_items {
  id               String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  order_id         String?           @db.Uuid
  sub_order_id     String?           @db.Uuid
  product_id       String?           @db.Uuid
  variant_id       String?           @db.Uuid
  dealer_id        String?           @db.Uuid
  quantity         Int
  unit_price       Decimal           @db.Decimal(12, 2)
  total_price      Decimal           @db.Decimal(12, 2)
  created_at       DateTime?         @default(now()) @db.Timestamptz(6)
  dealers          dealers?          @relation(fields: [dealer_id], references: [id], onUpdate: NoAction)
  orders           orders?           @relation(fields: [order_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  products         products?         @relation(fields: [product_id], references: [id], onUpdate: NoAction)
  sub_orders       sub_orders?       @relation(fields: [sub_order_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  product_variants product_variants? @relation(fields: [variant_id], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model order_notes {
  id           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  order_id     String?   @db.Uuid
  sub_order_id String?   @db.Uuid
  note         String
  is_internal  Boolean?  @default(true)
  created_by   String?   @db.Uuid
  created_at   DateTime? @default(now()) @db.Timestamptz(6)
  profiles     profiles? @relation(fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model order_status_history {
  id           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  order_id     String?   @db.Uuid
  sub_order_id String?   @db.Uuid
  status       String    @db.VarChar(20)
  comment      String?
  changed_by   String?   @db.Uuid
  created_at   DateTime? @default(now()) @db.Timestamptz(6)
  profiles     profiles? @relation(fields: [changed_by], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model orders {
  id                  String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  order_number        String              @unique @db.VarChar(50)
  user_id             String?             @db.Uuid
  status              String?             @default("pending") @db.VarChar(20)
  shipping_address_id String?             @db.Uuid
  shipping_name       String?             @db.VarChar(255)
  shipping_phone      String?             @db.VarChar(20)
  shipping_address    String?
  subtotal            Decimal             @db.Decimal(12, 2)
  discount_amount     Decimal?            @default(0) @db.Decimal(10, 2)
  shipping_cost       Decimal?            @default(0) @db.Decimal(10, 2)
  tax_amount          Decimal?            @default(0) @db.Decimal(10, 2)
  grand_total         Decimal             @db.Decimal(12, 2)
  payment_status      String?             @default("pending") @db.VarChar(20)
  payment_method      String?             @db.VarChar(50)
  notes               String?
  created_at          DateTime?           @default(now()) @db.Timestamptz(6)
  updated_at          DateTime?           @default(now()) @db.Timestamptz(6)
  commissions         commissions[]
  order_items         order_items[]
  customer_addresses  customer_addresses? @relation(fields: [shipping_address_id], references: [id], onUpdate: NoAction)
  profiles            profiles?           @relation(fields: [user_id], references: [id], onUpdate: NoAction)
  payments            payments[]
  refunds             refunds[]
  reviews             reviews[]
  sales               sales[]
  sub_orders          sub_orders[]

  @@index([user_id], map: "idx_orders_user")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model otp_verifications {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id        String?   @db.Uuid
  phone_or_email String    @db.VarChar(255)
  otp_code       String    @db.VarChar(10)
  purpose        String    @db.VarChar(50)
  expires_at     DateTime  @db.Timestamptz(6)
  is_used        Boolean?  @default(false)
  created_at     DateTime? @default(now()) @db.Timestamptz(6)
  profiles       profiles? @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model pages {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  title            String    @db.VarChar(255)
  slug             String    @unique @db.VarChar(255)
  content          String
  meta_title       String?   @db.VarChar(255)
  meta_description String?
  is_active        Boolean?  @default(true)
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  updated_at       DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model password_resets {
  id         String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id    String?   @db.Uuid
  token      String    @unique @db.VarChar(255)
  expires_at DateTime  @db.Timestamptz(6)
  created_at DateTime? @default(now()) @db.Timestamptz(6)
  profiles   profiles? @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model payment_transactions {
  id                                                  String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  transaction_number                                  String     @db.VarChar
  dealer_id                                           String     @db.Uuid
  transaction_type                                    String?    @db.VarChar
  reference_type                                      String?    @db.VarChar
  reference_id                                        String?    @db.Uuid
  reference_number                                    String?    @db.VarChar
  vendor_id                                           String?    @db.Uuid
  customer_id                                         String?    @db.Uuid
  payment_method                                      String?    @db.VarChar
  amount                                              Decimal    @db.Decimal
  bank_name                                           String?    @db.VarChar
  account_number                                      String?    @db.VarChar
  transaction_id                                      String?    @db.VarChar
  cheque_number                                       String?    @db.VarChar
  cheque_date                                         DateTime?  @db.Date
  status                                              String?    @default("completed") @db.VarChar
  notes                                               String?
  received_by                                         String?    @db.Uuid
  payment_date                                        DateTime   @default(dbgenerated("CURRENT_DATE")) @db.Date
  created_at                                          DateTime?  @default(now()) @db.Timestamptz(6)
  expenses                                            expenses[]
  profiles_payment_transactions_customer_idToprofiles profiles?  @relation("payment_transactions_customer_idToprofiles", fields: [customer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  dealers                                             dealers    @relation(fields: [dealer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  profiles_payment_transactions_received_byToprofiles profiles?  @relation("payment_transactions_received_byToprofiles", fields: [received_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  vendors                                             vendors?   @relation(fields: [vendor_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([dealer_id, transaction_number])
  @@index([payment_date(sort: Desc)], map: "idx_pt_date")
  @@index([dealer_id], map: "idx_pt_dealer")
  @@index([transaction_type], map: "idx_pt_type")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model payments {
  id                     String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  order_id               String?   @db.Uuid
  amount                 Decimal   @db.Decimal(12, 2)
  method                 String    @db.VarChar(50)
  status                 String?   @default("pending") @db.VarChar(20)
  gateway_transaction_id String?   @db.VarChar(100)
  gateway_response       Json?
  created_at             DateTime? @default(now()) @db.Timestamptz(6)
  updated_at             DateTime? @default(now()) @db.Timestamptz(6)
  orders                 orders?   @relation(fields: [order_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  refunds                refunds[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model payout_items {
  id             String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  payout_id      String?         @db.Uuid
  sub_order_id   String?         @db.Uuid
  amount         Decimal         @db.Decimal(12, 2)
  created_at     DateTime?       @default(now()) @db.Timestamptz(6)
  dealer_payouts dealer_payouts? @relation(fields: [payout_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  sub_orders     sub_orders?     @relation(fields: [sub_order_id], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model permissions {
  id               String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name             String             @unique @db.VarChar(100)
  module           String             @db.VarChar(50)
  description      String?
  created_at       DateTime?          @default(now()) @db.Timestamptz(6)
  role_permissions role_permissions[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model plan_features {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  plan_id       String?   @db.Uuid
  feature_name  String    @db.VarChar(100)
  feature_value String?
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  plans         plans?    @relation(fields: [plan_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model plans {
  id              String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name            String          @unique @db.VarChar(50)
  price           Decimal         @db.Decimal(10, 2)
  max_products    Int?
  max_users       Int?
  commission_rate Decimal         @db.Decimal(5, 2)
  description     String?
  created_at      DateTime?       @default(now()) @db.Timestamptz(6)
  updated_at      DateTime?       @default(now()) @db.Timestamptz(6)
  dealers         dealers[]
  plan_features   plan_features[]
  subscriptions   subscriptions[]

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model product_attribute_definitions {
  id                     String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  attribute_set_id       String?                 @db.Uuid
  name                   String                  @db.VarChar
  type                   String                  @db.VarChar
  options                Json?
  is_required            Boolean?                @default(false)
  is_variant_attribute   Boolean?                @default(false)
  display_order          Int?                    @default(0)
  created_at             DateTime?               @default(now()) @db.Timestamptz(6)
  product_attribute_sets product_attribute_sets? @relation(fields: [attribute_set_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([attribute_set_id], map: "idx_attr_defs_set")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model product_attribute_sets {
  id                            String                          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                          String                          @db.VarChar
  category_id                   String?                         @db.Uuid
  created_at                    DateTime?                       @default(now()) @db.Timestamptz(6)
  product_attribute_definitions product_attribute_definitions[]
  categories                    categories?                     @relation(fields: [category_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([category_id], map: "idx_attr_sets_category")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model product_attributes {
  id         String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name       String    @db.VarChar(100)
  type       String?   @db.VarChar(50)
  created_at DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model product_bike_models {
  product_id    String      @db.Uuid
  bike_model_id String      @db.Uuid
  bike_models   bike_models @relation(fields: [bike_model_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  products      products    @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([product_id, bike_model_id])
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model product_images {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  product_id    String?   @db.Uuid
  image_url     String
  is_primary    Boolean?  @default(false)
  display_order Int?      @default(0)
  created_at    DateTime? @default(now()) @db.Timestamptz(6)
  products      products? @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model product_variants {
  id                     String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  product_id             String?                  @db.Uuid
  sku                    String?                  @unique @db.VarChar(100)
  price                  Decimal?                 @db.Decimal(12, 2)
  stock_quantity         Int?                     @default(0)
  attributes             Json?
  created_at             DateTime?                @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?                @default(now()) @db.Timestamptz(6)
  barcode                String?                  @db.VarChar
  low_stock_threshold    Int?                     @default(5)
  mrp                    Decimal?                 @db.Decimal
  weight                 Decimal?                 @db.Decimal
  bin                    String?                  @db.VarChar
  stock_location_id      String?                  @db.Uuid
  has_duplicate_barcode  Boolean?                 @default(false)
  cart_items             cart_items[]
  inventory              inventory[]
  inventory_batches      inventory_batches[]
  inventory_movements    inventory_movements[]
  order_items            order_items[]
  products               products?                @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  stock_locations        stock_locations?         @relation(fields: [stock_location_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  purchase_order_items   purchase_order_items[]
  sale_items             sale_items[]
  stock_adjustment_items stock_adjustment_items[]

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model products {
  id                                              String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  dealer_id                                       String?                  @db.Uuid
  name                                            String                   @db.VarChar(255)
  slug                                            String                   @unique @db.VarChar(255)
  sku                                             String?                  @unique @db.VarChar(100)
  part_number                                     String?                  @db.VarChar(100)
  category_id                                     String?                  @db.Uuid
  sub_category_id                                 String?                  @db.Uuid
  brand                                           String?                  @db.VarChar(100)
  short_description                               String?
  description                                     String?
  specifications                                  Json?
  base_price                                      Decimal                  @db.Decimal(12, 2)
  sale_price                                      Decimal?                 @db.Decimal(12, 2)
  cost_price                                      Decimal?                 @db.Decimal(12, 2)
  discount_type                                   String?                  @default("none") @db.VarChar(20)
  stock_quantity                                  Int?                     @default(0)
  low_stock_threshold                             Int?                     @default(5)
  stock_status                                    String?                  @default("in_stock") @db.VarChar(20)
  weight                                          Decimal?                 @db.Decimal(10, 2)
  length                                          Decimal?                 @db.Decimal(10, 2)
  width                                           Decimal?                 @db.Decimal(10, 2)
  height                                          Decimal?                 @db.Decimal(10, 2)
  is_free_shipping                                Boolean?                 @default(false)
  status                                          String?                  @default("pending") @db.VarChar(20)
  submitted_at                                    DateTime?                @db.Timestamptz(6)
  approved_at                                     DateTime?                @db.Timestamptz(6)
  approved_by                                     String?                  @db.Uuid
  rejection_reason                                String?
  is_featured                                     Boolean?                 @default(false)
  is_new_arrival                                  Boolean?                 @default(true)
  is_best_seller                                  Boolean?                 @default(false)
  is_genuine                                      Boolean?                 @default(true)
  total_sold                                      Int?                     @default(0)
  total_views                                     Int?                     @default(0)
  average_rating                                  Decimal?                 @default(0) @db.Decimal(3, 2)
  review_count                                    Int?                     @default(0)
  created_at                                      DateTime?                @default(now()) @db.Timestamptz(6)
  updated_at                                      DateTime?                @default(now()) @db.Timestamptz(6)
  brand_id                                        String?                  @db.Uuid
  reorder_level                                   Int?                     @default(15)
  dimensions                                      Json?
  warranty_days                                   Int?                     @default(0)
  barcode                                         String?                  @unique
  cart_items                                      cart_items[]
  inventory                                       inventory[]
  inventory_batches                               inventory_batches[]
  inventory_movements                             inventory_movements[]
  order_items                                     order_items[]
  product_bike_models                             product_bike_models[]
  product_images                                  product_images[]
  product_variants                                product_variants[]
  profiles                                        profiles?                @relation(fields: [approved_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  brands                                          brands?                  @relation(fields: [brand_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  categories_products_category_idTocategories     categories?              @relation("products_category_idTocategories", fields: [category_id], references: [id], onUpdate: NoAction)
  dealers                                         dealers?                 @relation(fields: [dealer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  categories_products_sub_category_idTocategories categories?              @relation("products_sub_category_idTocategories", fields: [sub_category_id], references: [id], onUpdate: NoAction)
  purchase_order_items                            purchase_order_items[]
  reviews                                         reviews[]
  sale_items                                      sale_items[]
  service_requisitions                            service_requisitions[]
  stock_adjustment_items                          stock_adjustment_items[]
  wishlists                                       wishlists[]

  @@index([brand_id], map: "idx_products_brand")
  @@index([category_id], map: "idx_products_category")
  @@index([dealer_id], map: "idx_products_dealer")
  @@index([status], map: "idx_products_status")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model profiles {
  id                                                              String                  @id @db.Uuid
  email                                                           String?                 @unique @default("") @db.VarChar(255)
  phone                                                           String?                 @unique @db.VarChar(20)
  first_name                                                      String?                 @db.VarChar(100)
  last_name                                                       String?                 @db.VarChar(100)
  role_id                                                         String?                 @db.Uuid
  dealer_id                                                       String?                 @db.Uuid
  status                                                          String?                 @default("pending") @db.VarChar(20)
  email_verified                                                  Boolean?                @default(false)
  mfa_enabled                                                     Boolean?                @default(false)
  created_at                                                      DateTime?               @default(now()) @db.Timestamptz(6)
  updated_at                                                      DateTime?               @default(now()) @db.Timestamptz(6)
  full_name                                                       String?
  role                                                            String?
  onboarding_completed                                            Boolean?                @default(false)
  temp_password                                                   String?
  password_hash                                                   String?
  phone_verified                                                  Boolean?                @default(false)
  mfa_secret                                                      String?
  last_login_at                                                   DateTime?               @db.Timestamptz(6)
  failed_login_attempts                                           Int?                    @default(0)
  locked_until                                                    DateTime?               @db.Timestamptz(6)
  password_changed_at                                             DateTime?               @db.Timestamptz(6)
  audit_logs                                                      audit_logs[]
  auth_providers                                                  auth_providers[]
  business_unit_users                                             business_unit_users[]
  carts                                                           carts[]
  customer_addresses                                              customer_addresses[]
  customer_ledger                                                 customer_ledger[]
  dealer_documents                                                dealer_documents[]
  dealer_payouts                                                  dealer_payouts[]
  dealer_reviews                                                  dealer_reviews[]
  dealer_users                                                    dealer_users[]
  dealers_dealers_owner_user_idToprofiles                         dealers[]               @relation("dealers_owner_user_idToprofiles")
  expenses_expenses_approved_byToprofiles                         expenses[]              @relation("expenses_approved_byToprofiles")
  expenses_expenses_created_byToprofiles                          expenses[]              @relation("expenses_created_byToprofiles")
  inventory_movements                                             inventory_movements[]
  login_history                                                   login_history[]
  notifications                                                   notifications[]
  order_notes                                                     order_notes[]
  order_status_history                                            order_status_history[]
  orders                                                          orders[]
  otp_verifications                                               otp_verifications[]
  password_resets                                                 password_resets[]
  payment_transactions_payment_transactions_customer_idToprofiles payment_transactions[]  @relation("payment_transactions_customer_idToprofiles")
  payment_transactions_payment_transactions_received_byToprofiles payment_transactions[]  @relation("payment_transactions_received_byToprofiles")
  products                                                        products[]
  dealers_profiles_dealer_idTodealers                             dealers?                @relation("profiles_dealer_idTodealers", fields: [dealer_id], references: [id], onUpdate: NoAction)
  roles                                                           roles?                  @relation(fields: [role_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  purchase_orders_purchase_orders_created_byToprofiles            purchase_orders[]       @relation("purchase_orders_created_byToprofiles")
  purchase_orders_purchase_orders_received_byToprofiles           purchase_orders[]       @relation("purchase_orders_received_byToprofiles")
  push_subscriptions                                              push_subscriptions[]
  refresh_tokens_public                                           refresh_tokens_public[]
  refunds                                                         refunds[]
  review_replies                                                  review_replies[]
  reviews                                                         reviews[]
  sales_sales_customer_idToprofiles                               sales[]                 @relation("sales_customer_idToprofiles")
  sales_sales_sold_byToprofiles                                   sales[]                 @relation("sales_sold_byToprofiles")
  service_staff                                                   service_staff[]
  service_tickets                                                 service_tickets[]
  service_vehicles                                                service_vehicles[]
  stock_adjustments_stock_adjustments_approved_byToprofiles       stock_adjustments[]     @relation("stock_adjustments_approved_byToprofiles")
  stock_adjustments_stock_adjustments_performed_byToprofiles      stock_adjustments[]     @relation("stock_adjustments_performed_byToprofiles")
  support_tickets                                                 support_tickets[]
  ticket_messages                                                 ticket_messages[]
  transactions                                                    transactions[]
  user_sessions                                                   user_sessions[]
  wishlists                                                       wishlists[]

  @@index([dealer_id], map: "idx_profiles_dealer")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model promotions {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name           String    @db.VarChar(255)
  description    String?
  discount_type  String?   @db.VarChar(20)
  discount_value Decimal?  @db.Decimal(12, 2)
  starts_at      DateTime? @db.Timestamptz(6)
  ends_at        DateTime? @db.Timestamptz(6)
  is_active      Boolean?  @default(true)
  created_at     DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model purchase_order_items {
  id                 String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  purchase_order_id  String?             @db.Uuid
  product_id         String              @db.Uuid
  variant_id         String?             @db.Uuid
  barcode            String?             @db.VarChar
  batch_number       String?             @db.VarChar
  ordered_quantity   Int
  received_quantity  Int?                @default(0)
  damaged_quantity   Int?                @default(0)
  accepted_quantity  Int?                @default(dbgenerated("(received_quantity - damaged_quantity)"))
  unit_cost_price    Decimal             @db.Decimal
  total_cost         Decimal?            @default(dbgenerated("((ordered_quantity)::numeric * unit_cost_price)")) @db.Decimal
  discount_type      String?             @db.VarChar
  discount_value     Decimal?            @default(0) @db.Decimal
  discount_amount    Decimal?            @default(0) @db.Decimal
  net_amount         Decimal?            @db.Decimal
  quality_status     String?             @default("pending") @db.VarChar
  quality_notes      String?
  manufacturing_date DateTime?           @db.Date
  expiry_date        DateTime?           @db.Date
  notes              String?
  created_at         DateTime?           @default(now()) @db.Timestamptz(6)
  inventory_batches  inventory_batches[]
  products           products            @relation(fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  purchase_orders    purchase_orders?    @relation(fields: [purchase_order_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  product_variants   product_variants?   @relation(fields: [variant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([purchase_order_id], map: "idx_poi_po")
  @@index([product_id], map: "idx_poi_product")
  @@index([variant_id], map: "idx_poi_variant")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model purchase_orders {
  id                                             String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  po_number                                      String                 @db.VarChar
  dealer_id                                      String                 @db.Uuid
  vendor_id                                      String                 @db.Uuid
  order_date                                     DateTime               @default(dbgenerated("CURRENT_DATE")) @db.Date
  expected_delivery_date                         DateTime?              @db.Date
  received_date                                  DateTime?              @db.Date
  subtotal                                       Decimal                @default(0) @db.Decimal
  tax_amount                                     Decimal?               @default(0) @db.Decimal
  discount_amount                                Decimal?               @default(0) @db.Decimal
  shipping_cost                                  Decimal?               @default(0) @db.Decimal
  other_charges                                  Decimal?               @default(0) @db.Decimal
  grand_total                                    Decimal                @db.Decimal
  paid_amount                                    Decimal?               @default(0) @db.Decimal
  due_amount                                     Decimal?               @default(dbgenerated("(grand_total - paid_amount)")) @db.Decimal
  status                                         String?                @default("draft") @db.VarChar
  payment_status                                 String?                @default("unpaid") @db.VarChar
  invoice_number                                 String?                @db.VarChar
  invoice_file_url                               String?
  warehouse_location                             String?                @db.VarChar
  received_by                                    String?                @db.Uuid
  created_by                                     String?                @db.Uuid
  notes                                          String?
  internal_notes                                 String?
  created_at                                     DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at                                     DateTime?              @default(now()) @db.Timestamptz(6)
  inventory_batches                              inventory_batches[]
  purchase_order_items                           purchase_order_items[]
  profiles_purchase_orders_created_byToprofiles  profiles?              @relation("purchase_orders_created_byToprofiles", fields: [created_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  dealers                                        dealers                @relation(fields: [dealer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  profiles_purchase_orders_received_byToprofiles profiles?              @relation("purchase_orders_received_byToprofiles", fields: [received_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  vendors                                        vendors                @relation(fields: [vendor_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([dealer_id, po_number])
  @@index([order_date(sort: Desc)], map: "idx_po_date")
  @@index([dealer_id], map: "idx_po_dealer")
  @@index([dealer_id, status], map: "idx_po_status")
  @@index([vendor_id], map: "idx_po_vendor")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model push_subscriptions {
  id           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id      String?   @db.Uuid
  device_token String
  device_type  String?   @db.VarChar(50)
  created_at   DateTime? @default(now()) @db.Timestamptz(6)
  profiles     profiles? @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model refunds {
  id           String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  order_id     String?     @db.Uuid
  sub_order_id String?     @db.Uuid
  payment_id   String?     @db.Uuid
  amount       Decimal     @db.Decimal(12, 2)
  status       String?     @default("pending") @db.VarChar(20)
  type         String?     @db.VarChar(50)
  reason       String?
  processed_at DateTime?   @db.Timestamptz(6)
  processed_by String?     @db.Uuid
  created_at   DateTime?   @default(now()) @db.Timestamptz(6)
  orders       orders?     @relation(fields: [order_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  payments     payments?   @relation(fields: [payment_id], references: [id], onUpdate: NoAction)
  profiles     profiles?   @relation(fields: [processed_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  sub_orders   sub_orders? @relation(fields: [sub_order_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model review_replies {
  id         String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  review_id  String?   @db.Uuid
  user_id    String?   @db.Uuid
  reply      String
  created_at DateTime? @default(now()) @db.Timestamptz(6)
  reviews    reviews?  @relation(fields: [review_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles   profiles? @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model reviews {
  id                   String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  product_id           String?          @db.Uuid
  user_id              String?          @db.Uuid
  order_id             String?          @db.Uuid
  rating               Int?
  comment              String?
  status               String?          @default("pending") @db.VarChar(20)
  is_verified_purchase Boolean?         @default(false)
  created_at           DateTime?        @default(now()) @db.Timestamptz(6)
  review_replies       review_replies[]
  orders               orders?          @relation(fields: [order_id], references: [id], onUpdate: NoAction)
  products             products?        @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles             profiles?        @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model role_permissions {
  role_id       String      @db.Uuid
  permission_id String      @db.Uuid
  permissions   permissions @relation(fields: [permission_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  roles         roles       @relation(fields: [role_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([role_id, permission_id])
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model roles {
  id               String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name             String             @unique @db.VarChar(50)
  display_name     String             @db.VarChar(100)
  display_name_bn  String?            @db.VarChar(100)
  level            Int
  role_type        String?            @db.VarChar(20)
  is_system_role   Boolean?           @default(false)
  created_at       DateTime?          @default(now()) @db.Timestamptz(6)
  updated_at       DateTime?          @default(now()) @db.Timestamptz(6)
  profiles         profiles[]
  role_permissions role_permissions[]

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sale_items {
  id                   String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  sale_id              String?            @db.Uuid
  product_id           String             @db.Uuid
  variant_id           String?            @db.Uuid
  batch_id             String?            @db.Uuid
  batch_number         String?            @db.VarChar
  product_name         String             @db.VarChar
  variant_name         String?            @db.VarChar
  sku                  String?            @db.VarChar
  barcode              String?            @db.VarChar
  quantity             Int
  returned_quantity    Int?               @default(0)
  unit_cost_price      Decimal            @db.Decimal
  unit_selling_price   Decimal            @db.Decimal
  mrp                  Decimal?           @db.Decimal
  subtotal             Decimal?           @default(dbgenerated("((quantity)::numeric * unit_selling_price)")) @db.Decimal
  discount_amount      Decimal?           @default(0) @db.Decimal
  tax_amount           Decimal?           @default(0) @db.Decimal
  total_amount         Decimal?           @db.Decimal
  unit_profit          Decimal?           @default(dbgenerated("(unit_selling_price - unit_cost_price)")) @db.Decimal
  total_profit         Decimal?           @default(dbgenerated("((unit_selling_price - unit_cost_price) * ((quantity - returned_quantity))::numeric)")) @db.Decimal
  warranty_period_days Int?
  warranty_expires_at  DateTime?          @db.Date
  status               String?            @default("sold") @db.VarChar
  created_at           DateTime?          @default(now()) @db.Timestamptz(6)
  inventory_batches    inventory_batches? @relation(fields: [batch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  products             products           @relation(fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  sales                sales?             @relation(fields: [sale_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  product_variants     product_variants?  @relation(fields: [variant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([batch_id], map: "idx_si_batch")
  @@index([product_id], map: "idx_si_product")
  @@index([sale_id], map: "idx_si_sale")
  @@index([variant_id], map: "idx_si_variant")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sales {
  id                                   String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  sale_number                          String       @db.VarChar
  dealer_id                            String       @db.Uuid
  customer_id                          String?      @db.Uuid
  customer_name                        String?      @db.VarChar
  customer_phone                       String?      @db.VarChar
  customer_address                     String?
  sale_type                            String?      @default("retail") @db.VarChar
  order_id                             String?      @db.Uuid
  subtotal                             Decimal      @db.Decimal
  discount_type                        String?      @db.VarChar
  discount_value                       Decimal?     @default(0) @db.Decimal
  discount_amount                      Decimal?     @default(0) @db.Decimal
  tax_amount                           Decimal?     @default(0) @db.Decimal
  other_charges                        Decimal?     @default(0) @db.Decimal
  grand_total                          Decimal      @db.Decimal
  total_cost                           Decimal?     @db.Decimal
  total_profit                         Decimal?     @db.Decimal
  profit_margin                        Decimal?     @db.Decimal
  payment_method                       String?      @db.VarChar
  payment_status                       String?      @default("paid") @db.VarChar
  paid_amount                          Decimal?     @default(0) @db.Decimal
  due_amount                           Decimal?     @default(dbgenerated("(grand_total - paid_amount)")) @db.Decimal
  sale_date                            DateTime     @default(dbgenerated("CURRENT_DATE")) @db.Date
  sale_time                            DateTime     @default(dbgenerated("CURRENT_TIME")) @db.Time(6)
  sold_by                              String?      @db.Uuid
  status                               String?      @default("completed") @db.VarChar
  notes                                String?
  created_at                           DateTime?    @default(now()) @db.Timestamptz(6)
  updated_at                           DateTime?    @default(now()) @db.Timestamptz(6)
  sale_items                           sale_items[]
  profiles_sales_customer_idToprofiles profiles?    @relation("sales_customer_idToprofiles", fields: [customer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  dealers                              dealers      @relation(fields: [dealer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  orders                               orders?      @relation(fields: [order_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  profiles_sales_sold_byToprofiles     profiles?    @relation("sales_sold_byToprofiles", fields: [sold_by], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([dealer_id, sale_number])
  @@index([customer_id], map: "idx_sales_customer")
  @@index([sale_date(sort: Desc)], map: "idx_sales_date")
  @@index([dealer_id], map: "idx_sales_dealer")
  @@index([dealer_id, status], map: "idx_sales_status")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model settings {
  id          String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  key         String    @unique @db.VarChar(100)
  value       Json?
  description String?
  created_at  DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model shipping_methods {
  id             String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  zone_id        String?          @db.Uuid
  name           String           @db.VarChar(100)
  min_weight     Decimal?         @db.Decimal(10, 2)
  max_weight     Decimal?         @db.Decimal(10, 2)
  min_amount     Decimal?         @db.Decimal(12, 2)
  max_amount     Decimal?         @db.Decimal(12, 2)
  is_active      Boolean?         @default(true)
  created_at     DateTime?        @default(now()) @db.Timestamptz(6)
  shipping_zones shipping_zones?  @relation(fields: [zone_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  shipping_rates shipping_rates[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model shipping_rates {
  id               String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  method_id        String?           @db.Uuid
  rate             Decimal           @db.Decimal(10, 2)
  created_at       DateTime?         @default(now()) @db.Timestamptz(6)
  shipping_methods shipping_methods? @relation(fields: [method_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model shipping_zones {
  id               String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name             String             @db.VarChar(100)
  description      String?
  is_active        Boolean?           @default(true)
  created_at       DateTime?          @default(now()) @db.Timestamptz(6)
  shipping_methods shipping_methods[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sms_templates {
  id         String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name       String    @unique @db.VarChar(100)
  body_text  String?
  created_at DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model stock_adjustment_items {
  id                String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  adjustment_id     String?            @db.Uuid
  product_id        String?            @db.Uuid
  variant_id        String?            @db.Uuid
  batch_id          String?            @db.Uuid
  system_quantity   Int
  actual_quantity   Int
  difference        Int?               @default(dbgenerated("(actual_quantity - system_quantity)"))
  unit_cost         Decimal?           @db.Decimal
  value_impact      Decimal?           @default(dbgenerated("(((actual_quantity - system_quantity))::numeric * unit_cost)")) @db.Decimal
  reason            String?
  created_at        DateTime?          @default(now()) @db.Timestamptz(6)
  stock_adjustments stock_adjustments? @relation(fields: [adjustment_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  inventory_batches inventory_batches? @relation(fields: [batch_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  products          products?          @relation(fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  product_variants  product_variants?  @relation(fields: [variant_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([adjustment_id], map: "idx_sai_adjustment")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model stock_adjustments {
  id                                                String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  adjustment_number                                 String                   @db.VarChar
  dealer_id                                         String                   @db.Uuid
  adjustment_type                                   String?                  @db.VarChar
  status                                            String?                  @default("draft") @db.VarChar
  approved_by                                       String?                  @db.Uuid
  approved_at                                       DateTime?                @db.Timestamptz(6)
  rejection_reason                                  String?
  total_items                                       Int?                     @default(0)
  total_value_impact                                Decimal?                 @default(0) @db.Decimal
  reason                                            String
  notes                                             String?
  performed_by                                      String?                  @db.Uuid
  adjustment_date                                   DateTime                 @default(dbgenerated("CURRENT_DATE")) @db.Date
  created_at                                        DateTime?                @default(now()) @db.Timestamptz(6)
  stock_adjustment_items                            stock_adjustment_items[]
  profiles_stock_adjustments_approved_byToprofiles  profiles?                @relation("stock_adjustments_approved_byToprofiles", fields: [approved_by], references: [id], onDelete: NoAction, onUpdate: NoAction)
  dealers                                           dealers                  @relation(fields: [dealer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  profiles_stock_adjustments_performed_byToprofiles profiles?                @relation("stock_adjustments_performed_byToprofiles", fields: [performed_by], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([dealer_id, adjustment_number])
  @@index([dealer_id], map: "idx_sa_dealer")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model stock_locations {
  id               String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  dealer_id        String             @db.Uuid
  name             String             @db.VarChar
  code             String?            @db.VarChar
  description      String?
  is_default       Boolean?           @default(false)
  created_at       DateTime?          @default(now()) @db.Timestamptz(6)
  product_variants product_variants[]
  dealers          dealers            @relation(fields: [dealer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sub_orders {
  id                String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  order_id          String?        @db.Uuid
  dealer_id         String?        @db.Uuid
  status            String?        @default("pending") @db.VarChar(20)
  subtotal          Decimal        @db.Decimal(12, 2)
  commission_amount Decimal        @db.Decimal(12, 2)
  dealer_amount     Decimal        @db.Decimal(12, 2)
  shipping_cost     Decimal?       @default(0) @db.Decimal(10, 2)
  tracking_number   String?        @db.VarChar(100)
  shipped_at        DateTime?      @db.Timestamptz(6)
  delivered_at      DateTime?      @db.Timestamptz(6)
  created_at        DateTime?      @default(now()) @db.Timestamptz(6)
  updated_at        DateTime?      @default(now()) @db.Timestamptz(6)
  commissions       commissions[]
  order_items       order_items[]
  payout_items      payout_items[]
  refunds           refunds[]
  dealers           dealers?       @relation(fields: [dealer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  orders            orders?        @relation(fields: [order_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([dealer_id], map: "idx_sub_orders_dealer")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model subscription_invoices {
  id              String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  subscription_id String?        @db.Uuid
  dealer_id       String?        @db.Uuid
  amount          Decimal        @db.Decimal(12, 2)
  status          String?        @default("pending") @db.VarChar(20)
  billing_date    DateTime       @db.Timestamptz(6)
  paid_at         DateTime?      @db.Timestamptz(6)
  transaction_id  String?        @db.Uuid
  created_at      DateTime?      @default(now()) @db.Timestamptz(6)
  dealers         dealers?       @relation(fields: [dealer_id], references: [id], onUpdate: NoAction)
  subscriptions   subscriptions? @relation(fields: [subscription_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  transactions    transactions?  @relation(fields: [transaction_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model subscriptions {
  id                    String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  dealer_id             String?                 @db.Uuid
  plan_id               String?                 @db.Uuid
  status                String?                 @default("active") @db.VarChar(20)
  billing_cycle         String?                 @db.VarChar(20)
  starts_at             DateTime                @db.Timestamptz(6)
  ends_at               DateTime                @db.Timestamptz(6)
  cancelled_at          DateTime?               @db.Timestamptz(6)
  created_at            DateTime?               @default(now()) @db.Timestamptz(6)
  updated_at            DateTime?               @default(now()) @db.Timestamptz(6)
  subscription_invoices subscription_invoices[]
  dealers               dealers?                @relation(fields: [dealer_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  plans                 plans?                  @relation(fields: [plan_id], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model support_tickets {
  id              String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id         String?           @db.Uuid
  dealer_id       String?           @db.Uuid
  subject         String            @db.VarChar(255)
  status          String?           @default("open") @db.VarChar(20)
  priority        String?           @default("medium") @db.VarChar(20)
  created_at      DateTime?         @default(now()) @db.Timestamptz(6)
  updated_at      DateTime?         @default(now()) @db.Timestamptz(6)
  dealers         dealers?          @relation(fields: [dealer_id], references: [id], onUpdate: NoAction)
  profiles        profiles?         @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  ticket_messages ticket_messages[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ticket_attachments {
  id              String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  message_id      String?          @db.Uuid
  file_url        String
  file_type       String?          @db.VarChar(50)
  created_at      DateTime?        @default(now()) @db.Timestamptz(6)
  ticket_messages ticket_messages? @relation(fields: [message_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ticket_messages {
  id                 String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ticket_id          String?              @db.Uuid
  user_id            String?              @db.Uuid
  message            String
  is_internal        Boolean?             @default(false)
  created_at         DateTime?            @default(now()) @db.Timestamptz(6)
  ticket_attachments ticket_attachments[]
  support_tickets    support_tickets?     @relation(fields: [ticket_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles           profiles?            @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model transactions {
  id                    String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  type                  String                  @db.VarChar(50)
  amount                Decimal                 @db.Decimal(15, 2)
  status                String?                 @default("completed") @db.VarChar(20)
  reference_type        String?                 @db.VarChar(50)
  reference_id          String?                 @db.Uuid
  dealer_id             String?                 @db.Uuid
  user_id               String?                 @db.Uuid
  created_at            DateTime?               @default(now()) @db.Timestamptz(6)
  subscription_invoices subscription_invoices[]
  dealers               dealers?                @relation(fields: [dealer_id], references: [id], onUpdate: NoAction)
  profiles              profiles?               @relation(fields: [user_id], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_sessions {
  id                    String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id               String?                 @db.Uuid
  token                 String                  @unique
  ip_address            String?                 @db.VarChar(45)
  user_agent            String?
  last_active           DateTime?               @default(now()) @db.Timestamptz(6)
  expires_at            DateTime                @db.Timestamptz(6)
  created_at            DateTime?               @default(now()) @db.Timestamptz(6)
  refresh_token         String?                 @unique
  device_id             String?
  device_name           String?
  is_active             Boolean?                @default(true)
  login_method          String?                 @default("password") @db.VarChar(50)
  refresh_tokens_public refresh_tokens_public[]
  profiles              profiles?               @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([user_id, is_active], map: "idx_user_sessions_active")
  @@index([refresh_token], map: "idx_user_sessions_refresh")
  @@schema("public")
}

model auth_providers {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id          String    @db.Uuid
  provider         String    @db.VarChar(50)
  provider_user_id String
  provider_email   String?
  access_token     String?
  refresh_token    String?
  token_expires_at DateTime? @db.Timestamptz(6)
  raw_user_data    Json?
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  updated_at       DateTime? @default(now()) @db.Timestamptz(6)
  profiles         profiles  @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([provider, provider_user_id])
  @@schema("public")
}

model refresh_tokens_public {
  id             String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id        String         @db.Uuid
  token_hash     String         @unique
  session_id     String?        @db.Uuid
  family_id      String         @db.Uuid
  expires_at     DateTime       @db.Timestamptz(6)
  is_revoked     Boolean?       @default(false)
  revoked_at     DateTime?      @db.Timestamptz(6)
  revoked_reason String?
  created_at     DateTime?      @default(now()) @db.Timestamptz(6)
  user_sessions  user_sessions? @relation(fields: [session_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles       profiles       @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([family_id], map: "idx_refresh_tokens_family")
  @@index([token_hash], map: "idx_refresh_tokens_hash")
  @@index([user_id], map: "idx_refresh_tokens_user")
  @@map("refresh_tokens")
  @@schema("public")
}

model login_history {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id        String?   @db.Uuid
  email          String?
  login_type     String    @db.VarChar(50)
  ip_address     String?   @db.VarChar(50)
  user_agent     String?
  location       Json?
  status         String    @db.VarChar(20)
  failure_reason String?
  created_at     DateTime? @default(now()) @db.Timestamptz(6)
  profiles       profiles? @relation(fields: [user_id], references: [id], onUpdate: NoAction)

  @@index([created_at(sort: Desc)], map: "idx_login_history_created")
  @@index([email], map: "idx_login_history_email")
  @@index([user_id], map: "idx_login_history_user")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model vendor_ledger {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  dealer_id        String    @db.Uuid
  vendor_id        String    @db.Uuid
  transaction_type String?   @db.VarChar
  reference_type   String?   @db.VarChar
  reference_id     String?   @db.Uuid
  reference_number String?   @db.VarChar
  debit            Decimal?  @default(0) @db.Decimal
  credit           Decimal?  @default(0) @db.Decimal
  balance          Decimal?  @db.Decimal
  description      String?
  transaction_date DateTime  @default(dbgenerated("CURRENT_DATE")) @db.Date
  created_at       DateTime? @default(now()) @db.Timestamptz(6)
  dealers          dealers   @relation(fields: [dealer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  vendors          vendors   @relation(fields: [vendor_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([transaction_date(sort: Desc)], map: "idx_vl_date")
  @@index([vendor_id], map: "idx_vl_vendor")
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model vendors {
  id                   String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  dealer_id            String                 @db.Uuid
  name                 String                 @db.VarChar
  code                 String?                @db.VarChar
  contact_person       String?                @db.VarChar
  phone                String?                @db.VarChar
  email                String?                @db.VarChar
  address              String?
  credit_limit         Decimal?               @default(0) @db.Decimal
  current_balance      Decimal?               @default(0) @db.Decimal
  payment_terms        String?                @db.VarChar
  total_purchases      Decimal?               @default(0) @db.Decimal
  total_transactions   Int?                   @default(0)
  average_rating       Decimal?               @default(0) @db.Decimal
  status               String?                @default("active") @db.VarChar
  is_preferred         Boolean?               @default(false)
  notes                String?
  created_at           DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at           DateTime?              @default(now()) @db.Timestamptz(6)
  expenses             expenses[]
  inventory_batches    inventory_batches[]
  payment_transactions payment_transactions[]
  purchase_orders      purchase_orders[]
  vendor_ledger        vendor_ledger[]
  dealers              dealers                @relation(fields: [dealer_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([dealer_id, code])
  @@index([dealer_id], map: "idx_vendors_dealer")
  @@index([dealer_id, status], map: "idx_vendors_status")
  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model wishlists {
  id         String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  user_id    String?   @db.Uuid
  product_id String?   @db.Uuid
  created_at DateTime? @default(now()) @db.Timestamptz(6)
  products   products? @relation(fields: [product_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles   profiles? @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model service_ramps {
  id                                                               String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ramp_number                                                      Int               @unique
  status                                                           String            @default("idle")
  technician_name                                                  String?
  current_ticket_id                                                String?           @db.Uuid
  created_at                                                       DateTime?         @default(now()) @db.Timestamptz(6)
  updated_at                                                       DateTime?         @default(now()) @db.Timestamptz(6)
  staff_id                                                         String?           @db.Uuid
  service_tickets_service_ramps_current_ticket_idToservice_tickets service_tickets?  @relation("service_ramps_current_ticket_idToservice_tickets", fields: [current_ticket_id], references: [id], onUpdate: NoAction, map: "fk_current_ticket")
  service_staff                                                    service_staff?    @relation(fields: [staff_id], references: [id], onUpdate: NoAction)
  service_tickets_service_tickets_ramp_idToservice_ramps           service_tickets[] @relation("service_tickets_ramp_idToservice_ramps")

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model service_requisitions {
  id              String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ticket_id       String?          @db.Uuid
  staff_id        String?          @db.Uuid
  product_id      String?          @db.Uuid
  quantity        Int?             @default(1)
  status          String?          @default("pending")
  notes           String?
  created_at      DateTime?        @default(now()) @db.Timestamptz(6)
  updated_at      DateTime?        @default(now()) @db.Timestamptz(6)
  unit_price      Decimal?         @default(0) @db.Decimal
  total_price     Decimal?         @default(0) @db.Decimal
  products        products?        @relation(fields: [product_id], references: [id], onUpdate: NoAction)
  service_staff   service_staff?   @relation(fields: [staff_id], references: [id], onUpdate: NoAction)
  service_tickets service_tickets? @relation(fields: [ticket_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model service_staff {
  id                   String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                 String
  phone                String?
  is_active            Boolean?               @default(true)
  created_at           DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at           DateTime?              @default(now()) @db.Timestamptz(6)
  profile_id           String?                @db.Uuid
  staff_id             String?                @unique
  nid                  String?
  email                String?
  status               String?                @default("approved")
  designation          String?                @default("Technician")
  service_ramps        service_ramps[]
  service_requisitions service_requisitions[]
  profiles             profiles?              @relation(fields: [profile_id], references: [id], onUpdate: NoAction)
  service_tickets      service_tickets[]

  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model service_tickets {
  id                                                             String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  vehicle_id                                                     String?                @db.Uuid
  customer_id                                                    String?                @db.Uuid
  ramp_id                                                        String?                @db.Uuid
  status                                                         String                 @default("waiting")
  service_description                                            String?
  created_at                                                     DateTime?              @default(now()) @db.Timestamptz(6)
  updated_at                                                     DateTime?              @default(now()) @db.Timestamptz(6)
  service_number                                                 String?                @unique
  staff_id                                                       String?                @db.Uuid
  admin_notes                                                    String?
  finalized_at                                                   DateTime?              @db.Timestamptz(6)
  service_ramps_service_ramps_current_ticket_idToservice_tickets service_ramps[]        @relation("service_ramps_current_ticket_idToservice_tickets")
  service_requisitions                                           service_requisitions[]
  profiles                                                       profiles?              @relation(fields: [customer_id], references: [id], onUpdate: NoAction)
  service_ramps_service_tickets_ramp_idToservice_ramps           service_ramps?         @relation("service_tickets_ramp_idToservice_ramps", fields: [ramp_id], references: [id], onUpdate: NoAction)
  service_staff                                                  service_staff?         @relation(fields: [staff_id], references: [id], onUpdate: NoAction)
  service_vehicles                                               service_vehicles?      @relation(fields: [vehicle_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model service_vehicles {
  id                   String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  customer_id          String?           @db.Uuid
  engine_number        String            @unique
  chassis_number       String            @unique
  phone_number         String
  model_id             String?           @db.Uuid
  customer_name        String?
  created_at           DateTime?         @default(now()) @db.Timestamptz(6)
  updated_at           DateTime?         @default(now()) @db.Timestamptz(6)
  color                String?
  customer_id_custom   String?
  division             String?
  district_city        String?
  thana_upozilla       String?
  post_office          String?
  village_mahalla_para String?
  house_road_no        String?
  email                String?
  customer_nid         String?
  profession           String?
  date_of_birth        DateTime?         @db.Date
  weight_kgs           Decimal?          @db.Decimal
  gender               String?
  previous_bike_model  String?
  bike_driving_place   String?
  road_condition       String?
  date_of_purchase     DateTime?         @db.Date
  purchase_from        String?
  service_tickets      service_tickets[]
  profiles             profiles?         @relation(fields: [customer_id], references: [id], onUpdate: NoAction)
  bike_models          bike_models?      @relation(fields: [model_id], references: [id], onUpdate: NoAction)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model business_unit_users {
  id               String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  business_unit_id String?         @db.Uuid
  user_id          String?         @db.Uuid
  is_primary       Boolean?        @default(true)
  created_at       DateTime?       @default(now()) @db.Timestamptz(6)
  updated_at       DateTime?       @default(now()) @db.Timestamptz(6)
  business_units   business_units? @relation(fields: [business_unit_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  profiles         profiles?       @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([business_unit_id, user_id])
  @@schema("public")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model business_units {
  id                  String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                String                @db.VarChar(255)
  code                String                @unique @db.VarChar(50)
  unit_type           String                @db.VarChar(50)
  is_active           Boolean?              @default(true)
  created_at          DateTime?             @default(now()) @db.Timestamptz(6)
  updated_at          DateTime?             @default(now()) @db.Timestamptz(6)
  business_unit_users business_unit_users[]

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model job_cards {
  id                 String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  ticket_id          String?   @db.Uuid
  technician_id      String?   @db.Uuid
  status             String?   @default("in_progress") @db.VarChar(50)
  notes              String?
  service_start_time DateTime? @default(now()) @db.Timestamptz(6)
  service_end_time   DateTime? @db.Timestamptz(6)
  created_at         DateTime? @default(now()) @db.Timestamptz(6)
  updated_at         DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model part_variants {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  part_id        String?   @db.Uuid
  brand          String    @db.VarChar(100)
  sku            String?   @unique @db.VarChar(100)
  price          Decimal   @default(0) @db.Decimal(10, 2)
  stock_quantity Int?      @default(0)
  created_at     DateTime? @default(now()) @db.Timestamptz(6)
  updated_at     DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model parts {
  id          String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name        String    @db.VarChar(255)
  category_id String?   @db.Uuid
  code        String?   @unique @db.VarChar(50)
  created_at  DateTime? @default(now()) @db.Timestamptz(6)
  updated_at  DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model parts_usage {
  id          String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  job_card_id String?   @db.Uuid
  variant_id  String?   @db.Uuid
  quantity    Int       @default(1)
  unit_price  Decimal   @db.Decimal(10, 2)
  total_price Decimal?  @default(dbgenerated("((quantity)::numeric * unit_price)")) @db.Decimal(10, 2)
  created_at  DateTime? @default(now()) @db.Timestamptz(6)

  @@schema("public")
}

enum aal_level {
  aal1
  aal2
  aal3

  @@schema("auth")
}

enum code_challenge_method {
  s256
  plain

  @@schema("auth")
}

enum factor_status {
  unverified
  verified

  @@schema("auth")
}

enum factor_type {
  totp
  webauthn
  phone

  @@schema("auth")
}

enum oauth_authorization_status {
  pending
  approved
  denied
  expired

  @@schema("auth")
}

enum oauth_client_type {
  public
  confidential

  @@schema("auth")
}

enum oauth_registration_type {
  dynamic
  manual

  @@schema("auth")
}

enum oauth_response_type {
  code

  @@schema("auth")
}

enum one_time_token_type {
  confirmation_token
  reauthentication_token
  recovery_token
  email_change_token_new
  email_change_token_current
  phone_change_token

  @@schema("auth")
}
