declare module 'lucide-react-native/dist/esm/icons/*' {
  import { LucideProps } from 'lucide-react-native';
  import { ReactNode } from 'react';
  const Icon: (props: LucideProps) => ReactNode;
  export default Icon;
}
